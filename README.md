# Лабораторная работа 11: Итераторы и генераторы

## Тема: Итераторы и генераторы в Python

### Задания:

1. **Генератор чисел Фибоначчи** - реализация функции `fib(n)` с использованием `yield`
2. **Сохранение в файл** - запись каждого числа Фибоначчи в файл `fib.txt`

### Реализация:

- `fib(n)` - генератор чисел Фибоначчи
- `fib_with_save(n, filename)` - генератор с сохранением в файл
- Примеры использования итераторов и генераторов

### Запуск:

```bash
python main.py
